(function () {
  if (typeof window.Pixels === 'undefined') {
    window.Pixels = {};
  }

  Palette = Pixels.Palette = function () {
    this.colors = ['red', 'cornflowerblue', 'forestgreen'];
  };
})();
